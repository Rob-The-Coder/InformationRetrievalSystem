package it.unipv.compeng.model.postinglist;

import it.unipv.compeng.model.term.Term;
public class SkippingPostingList extends PostingList{
  //Maybe is better to create an interface ISkippable that has a method like skip() and has as variable the skipping amount;
  @Override
  public void addToPostingList(Term t){

  }
}
